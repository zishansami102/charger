// ----------------------------------------------------------------------

export const tokenImages = (symbol) => `/static/images/tokens/token_${symbol}.png`;
export const walletImages = (name) => `/static/images/wallets/wallet_${name}.png`;
