// routes
import Router from './routes';
// theme
import ThemeConfig from './theme';

// ----------------------------------------------------------------------

export default function App() {
  return (
    <ThemeConfig>
      <Router />
    </ThemeConfig>
  );
}
